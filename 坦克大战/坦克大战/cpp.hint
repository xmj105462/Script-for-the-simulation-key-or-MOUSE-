// 提示文件帮助 Visual Studio IDE 解释 Visual C++ 标识符,
// 如函数和宏的名称。
// 有关详细信息，请参见 https://go.microsoft.com/fwlink/?linkid=865984
#define TANK_INIT(T1, T2) map[T1.y][T1.x] = T2; map[T1.y][T1.x+1] = T2;map[T1.y + 1][T1.x] = T2; map[T1.y + 1][T1.x + 1] = T2;
#define LOAD_BLAST(T1) loadimage(&blastImg[T1-1], _T("res/blastT1.gif"), IMAGE_SIZE<<1, 47, true);
#define DELETE_B
#define SHELL2 clearrectangle(biu->x * IMAGE_SIZE, biu->y * IMAGE_SIZE, (biu->x + 1) * IMAGE_SIZE, (biu->y + 2) * IMAGE_SIZE);map[biu->y][biu->x] = 0; map[biu->y+1][biu->x] = 0; delete biu; biu = nullptr;
#define SHELL2 clearrectangle(biu->x * IMAGE_SIZE, biu->y * IMAGE_SIZE, (biu->x + 1) * IMAGE_SIZE, (biu->y + 2) * IMAGE_SIZE);map[biu->y][biu->x] = 0; map[biu->y+1][biu->x] = 0; delete biu; biu = nullptr;
#define SHELL2 clearrectangle(biu->x * IMAGE_SIZE, biu->y * IMAGE_SIZE, (biu->x + 1) * IMAGE_SIZE, (biu->y + 2) * IMAGE_SIZE);map[biu->y][biu->x] = 0; map[biu->y+1][biu->x] = 0; delete biu; biu = nullptr;
